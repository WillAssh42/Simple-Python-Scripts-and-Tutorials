# how to use time statements
import time # when you import time this allows you to use the wait() command
# here is an example
wait(1) # the number = the amount of seconds it waits
print("hello world!") # this is what happens after the 1 second ( or however long you choose it to be )
