# this is where you can use the random statement to generate a number, the number can be any number in existance 
import random # this allows you too use the random.randint(,) statement
number = random.randint(1,100) # here you can change 1 or 100 to any number 
print (number) # this prints a random number
